<!-- Generated by h-alter. -->
# Core Beliefs

Rules belong in tools first. Prose explains why the rule exists; the formatter,
linter, hook, typechecker, test, or validator enforces it.

Add guardrails only when real failures justify them. Preserve human-authored
content and ask on conflict.
